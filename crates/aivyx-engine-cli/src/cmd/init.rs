//! Legacy init command — now delegates to [`super::genesis`].
//!
//! The `aivyx init` command is dispatched directly to `genesis::run(true)` in
//! `main.rs`. This module is retained for documentation purposes only.
